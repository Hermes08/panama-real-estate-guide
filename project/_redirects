# Netlify _redirects — only legacy slug redirects remain.
# All per-language pages now exist as real static files:
#   /es/articles/ /es/news/ /es/videos/ (index pages)
#   /es/articles/<slug>.html /es/news/<slug>.html /es/projects/<slug>.html /es/videos/<id>.html
#   (same for /pt/ /de/)
# Netlify serves static files before checking _redirects, so we only need
# fallbacks for legacy URLs that were used in old translations.

# Legacy Spanish-slug privacy/terms (old DetailFooter pointed there before fix)
/privacidad  /privacy.html  301
/terminos    /terms.html    301
